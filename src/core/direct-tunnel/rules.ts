// import fs from "fs";
// export const BYPASS_RULES = fs
//   .readFileSync("rules/bypass.rules.txt", "utf-8")
//   .split("\n")
//   .map((r) => r.trim())
//   .filter(Boolean)
//   .map((r) => new RegExp(r, "i"));
